import Link from "next/link";
import { ArrowRight, BookOpen } from "lucide-react";
import { CTA, PageIntro, SectionHeading } from "@/components/sections";
import { subjectGroups } from "@/data/site";
export const metadata = { title: "Classes & Subjects", description: "Find personalized tuition support for primary, middle, secondary, and senior secondary subjects." };
export default function Classes() { return <><PageIntro eyebrow="Classes & subjects" title="A strong foundation for every next step." text="Explore subject support across key school stages. Content is organized to make finding the right focus simple."/><section className="container py-24"><SectionHeading eyebrow="Explore learning levels" title="Meet your child at their current stage." text="Subject coverage can be tailored to the school curriculum and goals you share with us."/><div className="mt-12 grid gap-5 md:grid-cols-2">{subjectGroups.map((group,index) => <article key={group.level} className="group rounded-[2rem] border border-slate-200 bg-white p-7 transition hover:-translate-y-1 hover:shadow-xl"><div className="flex items-start justify-between"><span className="grid size-12 place-items-center rounded-2xl bg-gradient-to-br from-blue-600 to-cyan-400 text-white"><BookOpen size={21}/></span><span className="text-sm font-extrabold text-blue-600">0{index+1}</span></div><h2 className="mt-9 text-2xl font-extrabold">{group.level}</h2><p className="mt-1 text-sm font-semibold text-slate-400">{group.years}</p><div className="mt-7 flex flex-wrap gap-2">{group.subjects.map(subject => <span key={subject} className="rounded-full bg-slate-100 px-3 py-2 text-xs font-bold text-slate-600">{subject}</span>)}</div></article>)}</div></section><section className="container pb-24"><div className="rounded-[2rem] border border-blue-100 bg-gradient-to-r from-blue-50 to-cyan-50 p-8 md:flex md:items-center md:justify-between md:p-12"><div><h2 className="text-2xl font-extrabold tracking-[-.03em] md:text-3xl">Not sure where to begin?</h2><p className="mt-2 text-sm leading-6 text-slate-500">Tell us the class, subject, and goal. We’ll help you find the right fit.</p></div><Link href="/contact" className="mt-6 inline-flex items-center gap-2 rounded-full bg-[#101b36] px-5 py-3 text-sm font-bold text-white md:mt-0">Find the Right Tutor <ArrowRight size={17}/></Link></div></section><CTA/></>; }
