import * as Icons from "lucide-react";
import { CTA, PageIntro, SectionHeading } from "@/components/sections";
import { reasons } from "@/data/site";
export const metadata = { title: "Why Choose Us", description: "Discover the thoughtful, personalized approach behind ANANYA ARUN TUITION." };
export default function WhyUs() { return <><PageIntro eyebrow="Why choose us" title="A tutor should bring more than answers." text="They should bring patience, perspective, and a way of explaining things that makes the next question easier to ask."/><section className="container py-24"><SectionHeading eyebrow="The Ananya Arun Tuition difference" title="Professional support, made personal." text="Our approach combines the structure families need with the flexibility students respond to."/><div className="mt-12 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{reasons.map(([title,text,icon], index) => { const Icon = (Icons as unknown as Record<string, React.ComponentType<{size?:number; className?:string}>>)[icon]; return <article key={title} className={`rounded-3xl p-7 ${index === 0 ? "bg-[#101b36] text-white" : "border border-slate-200 bg-white"}`}><Icon size={25} className={index === 0 ? "text-cyan-300" : "text-blue-600"}/><h2 className="mt-12 text-lg font-bold">{title}</h2><p className={`mt-3 text-sm leading-6 ${index === 0 ? "text-slate-300" : "text-slate-500"}`}>{text}</p></article>})}</div></section><CTA/></>; }
